/**
 * Public Cloudflare Turnstile **Site key** (vathala-web widget).
 *
 * Cloudflare shows two keys — only paste the **Site key** here:
 *   Turnstile → vathala-web → Overview → Site key
 *
 * Do NOT use the Secret key (that goes on the VaThala API server only).
 */
export const TURNSTILE_SITE_KEY = "0x4AAAAAADenlhAS5ZciAcWF";
